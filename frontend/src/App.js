import React from 'react';
import VersionUpdater from './components/VersionUpdater';

/* 
  Normally would add a linter to enforce code style and formatting rules, but not doing it here for simplicity for this demo app.
*/

function App() {
  return (
    <div className="App">
      <VersionUpdater />
    </div>
  );
}

export default App;